<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yub.system.mapper.system.SysBannerMapper">

    <resultMap id="BaseResultMap" type="com.yub.system.entity.system.SysBanner">
        <id column="id" property="id" />
        <result column="image_url" property="imageUrl" />
        <result column="link_url" property="linkUrl" />
        <result column="sort" property="sort" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="create_user" property="createUser" />
        <result column="update_time" property="updateTime" />
        <result column="update_user" property="updateUser" />
    </resultMap>

    <sql id="baseColumns">
        id, image_url, link_url, sort, status,
        create_time, create_user, update_time, update_user
    </sql>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sys_banner
        WHERE 1=1
        <if test="query.status != null">
            AND status = #{query.status}
        </if>
        ORDER BY sort ASC, id ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sys_banner
        WHERE id = #{id}
    </select>

    <select id="selectAllEnabled" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sys_banner
        WHERE status = 1
        ORDER BY sort ASC, id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_banner (image_url, link_url, sort, status,
                                 create_time, create_user, update_time, update_user)
        VALUES (#{imageUrl}, #{linkUrl}, #{sort}, #{status},
                NOW(), #{createUser}, NOW(), #{updateUser})
    </insert>

    <update id="updateById">
        UPDATE sys_banner
        SET image_url = #{imageUrl},
            link_url = #{linkUrl},
            sort = #{sort},
            status = #{status},
            update_time = NOW(),
            update_user = #{updateUser}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_banner WHERE id = #{id}
    </delete>
</mapper>
