<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yub.system.mapper.user.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.yub.system.entity.user.SysUser">
        <id column="id" property="id" />
        <result column="account" property="account" />
        <result column="password" property="password" />
        <result column="nick_name" property="nickName" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="avatar_url" property="avatarUrl" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="create_by" property="createBy" />
        <result column="update_by" property="updateBy" />
        <result column="deleted" property="deleted" />
        <result column="last_login_time" property="lastLoginTime" />
        <result column="last_login_ip" property="lastLoginIp" />
        <result column="login_count" property="loginCount" />
    </resultMap>

    <sql id="baseColumns">
        id, account, password, nick_name, phone, email, avatar_url, status,
        create_time, update_time, create_by, update_by, deleted, last_login_time, last_login_ip, login_count
    </sql>

    <select id="selectByAccount" resultType="com.yub.system.entity.user.SysUser">
        SELECT <include refid="baseColumns"/>
        FROM sys_user
        WHERE account = #{account} AND deleted = 0
    </select>

    <select id="selectById" resultType="com.yub.system.entity.user.SysUser">
        SELECT <include refid="baseColumns"/>
        FROM sys_user
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectRoleCodesByUserId" resultType="string">
        SELECT r.code
        FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.status = 1 AND r.deleted = 0
    </select>

    <update id="updateLoginInfo">
        UPDATE sys_user
        SET last_login_time = #{lastLoginTime}, last_login_ip = #{lastLoginIp}, login_count = #{loginCount}
        WHERE id = #{id}
    </update>

</mapper>
